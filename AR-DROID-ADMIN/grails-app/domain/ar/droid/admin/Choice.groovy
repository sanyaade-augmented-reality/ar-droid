package ar.droid.admin

class Choice {
	String value
	String description
	/**puede conocer a las preguntas que las contienen!! pero no se si es necesario
	 * una relacdion muchos a muchos con ChooseQuestion*/
	//static hasMany =[questions:Question]ç
		
    static constraints = {
		
    }
}
